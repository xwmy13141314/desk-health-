<Window x:Class="DeskHealth.App.Views.NotifyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="400"
        Height="180"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowActivated="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">

    <Border Background="#FFFFFF"
            Opacity="1.0"
            CornerRadius="8"
            BorderBrush="#CCCCCC"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 关闭按钮 -->
            <Button Grid.Row="0"
                    Content="×"
                    Width="24"
                    Height="24"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,4,8,0"
                    Background="Transparent"
                    BorderThickness="0"
                    FontSize="16"
                    Foreground="#666666"
                    Cursor="Hand"
                    Click="OnCloseClicked">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#FF0000"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- 单个提醒内容 -->
            <StackPanel x:Name="SingleReminderPanel"
                        Grid.Row="1"
                        VerticalAlignment="Center"
                        Margin="25,15">
                <TextBlock Text="{Binding Icon}"
                          FontSize="32"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,10"/>
                <TextBlock Text="{Binding Title}"
                          FontSize="18"
                          FontWeight="Bold"
                          HorizontalAlignment="Center"
                          Foreground="#1a1a1a"/>
                <TextBlock Text="{Binding Message}"
                          FontSize="13"
                          HorizontalAlignment="Center"
                          Foreground="#555555"
                          Margin="0,8,0,0"
                          TextWrapping="Wrap"/>
            </StackPanel>

            <!-- 组合提醒内容 -->
            <StackPanel x:Name="CombinedReminderPanel"
                        Grid.Row="1"
                        VerticalAlignment="Center"
                        Margin="25,15"
                        Visibility="Collapsed">
                <TextBlock Text="🌟"
                          FontSize="32"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,10"/>
                <TextBlock Text="健康提醒"
                          FontSize="18"
                          FontWeight="Bold"
                          HorizontalAlignment="Center"
                          Foreground="#1a1a1a"/>
                <TextBlock Text="该喝水了，起来活动一下吧！"
                          FontSize="13"
                          HorizontalAlignment="Center"
                          Foreground="#555555"
                          Margin="0,8,0,15"
                          TextWrapping="Wrap"/>
                <!-- 子提醒列表 -->
                <ItemsControl x:Name="SubRemindersItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#F5F5F5"
                                   CornerRadius="6"
                                   Padding="12,8"
                                   Margin="0,5,0,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                              Text="{Binding Icon}"
                                              FontSize="20"
                                              Margin="0,0,10,0"
                                              VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Title}"
                                                  FontSize="14"
                                                  FontWeight="SemiBold"
                                                  Foreground="#1a1a1a"/>
                                        <TextBlock Text="{Binding Message}"
                                                  FontSize="11"
                                                  Foreground="#666666"
                                                  Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </Border>
</Window>
